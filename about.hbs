# Third-party notices

The compiled extension module `tsecon._core` statically links the Rust
crates below. Each crate's licence text and copyright notice is reproduced
here as its licence requires. This file is generated by `cargo about`
(`scripts/gen_third_party_notices.sh`); do not edit it by hand. The
per-crate inventory with versions is `THIRD-PARTY-LICENSES.md` at the
repository root.

## Overview

{{#each overview}}
- {{{name}}} ({{id}}): {{count}} crate{{#if (ne count 1)}}s{{/if}}
{{/each}}

## Licences

{{#each licenses}}
### {{{name}}} ({{id}})

Used by:
{{#each used_by}}
- {{crate.name}} {{crate.version}}{{#if crate.repository}} — {{crate.repository}}{{/if}}
{{/each}}

```
{{{text}}}
```

{{/each}}
